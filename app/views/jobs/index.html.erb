<h1>職業一覧</h1>
<%= link_to '職業新規登録',  new_job_path %>

<% @jobs.each do |job| %>
 <div>
  <%= link_to job.name, job_path(job.id) %>
  <%= link_to job.user.name, user_path(job.user_id) %>
 </div>
<%end%>